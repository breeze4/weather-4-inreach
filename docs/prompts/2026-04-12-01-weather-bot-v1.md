# Orchestration Prompt: Weather Bot v1

## Project context

- Working directory: `/home/breeze/dev/weather-bot`
- Spec: `docs/specs/2026-04-12-01-weather-bot-v1.md`
- Handoff directory: `docs/handoff/` (create if needed)
- Build: N/A (no build step — pure Python)
- Test: `python -m pytest tests/ -v`
- Lint: N/A for v1
- Sample email fixture: `docs/inReach message from Jane Doe.eml`

## Orchestrator responsibilities

You are actively managing context between agents. Before launching each step:

1. Read the files listed under "Context sources" and include relevant sections in the agent's "Context" field.
2. If a previous step completed, read `docs/handoff/step-{N}-*.md` and use it to fill in what changed.
3. For Steps 1-3 (parallel): launch all three agents simultaneously. They have completely disjoint file sets.
4. For Step 4: wait for all three to complete, read all handoff files, then launch.
5. For Step 5 (HITL): pause for user confirmation before and after.

Create `docs/handoff/` and `src/` and `tests/` directories before launching any agents.

## Execution plan

### Steps 1-3 — Parallel: Core modules

Launch these three agents simultaneously. They own completely disjoint files and have no dependencies on each other.

---

### Step 1 — Email Ingestion + Parse

**Plan**: `docs/plans/2026-04-12-01-email-ingestion-parse.md`

**Agent briefing**:
- **Context sources** (orchestrator reads these): `docs/specs/2026-04-12-01-weather-bot-v1.md` (Data Flow steps 1-4, Behavior section), `docs/inReach message from Jane Doe.eml`
- **Read first**: `docs/plans/2026-04-12-01-email-ingestion-parse.md`
- **Context**: Orchestrator pastes the full sample `.eml` file content and the relevant spec sections here before launch.
- **Owns**: `src/email_poller.py`, `src/inreach_parser.py`, `tests/test_inreach_parser.py`, `.env.example`
- **Must not touch**: `src/nws_client.py`, `src/formatter.py`, `src/reply_sender.py`, `src/retry_manager.py`, `src/main.py`
- **Do not** implement the main loop, SMTP sending, or weather fetching — those are Steps 2-4's responsibility.
- **Done when**: Parser unit tests pass. `email_poller.py` has a working IMAP poll function. `.env.example` exists with `GMAIL_USER` and `GMAIL_APP_PASSWORD`.
- **Handoff**: Write `docs/handoff/step-1-email-ingestion.md` listing: files created, the parsed request dataclass shape (field names and types), and any decisions made about email parsing.

Stay within your plan's scope. If you see an improvement that belongs to a later step, leave it.

---

### Step 2 — NWS Forecast Fetch

**Plan**: `docs/plans/2026-04-12-02-nws-forecast-fetch.md`

**Agent briefing**:
- **Context sources** (orchestrator reads these): `docs/specs/2026-04-12-01-weather-bot-v1.md` (Weather APIs section, NWS subsection)
- **Read first**: `docs/plans/2026-04-12-02-nws-forecast-fetch.md`
- **Context**: Orchestrator pastes the NWS API details from the spec here before launch.
- **Owns**: `src/nws_client.py`
- **Must not touch**: `src/email_poller.py`, `src/inreach_parser.py`, `src/formatter.py`, `src/reply_sender.py`, `src/retry_manager.py`, `src/main.py`
- **Do not** implement email handling, formatting, or the main loop — those are other steps' responsibility.
- **Done when**: `fetch_forecast(lat, lon)` returns a list of forecast period dataclasses when called with test coordinates `(47.756259, -122.350252)`. Raises clear exceptions on API failure.
- **Handoff**: Write `docs/handoff/step-2-nws-fetch.md` listing: files created, the forecast period dataclass shape (field names and types), the function signature, and any NWS API quirks discovered (e.g., null precip values).

Stay within your plan's scope. If you see an improvement that belongs to a later step, leave it.

---

### Step 3 — Gemini Formatter + Validation

**Plan**: `docs/plans/2026-04-12-03-gemini-formatter.md`

**Agent briefing**:
- **Context sources** (orchestrator reads these): `docs/specs/2026-04-12-01-weather-bot-v1.md` (Message Formatting section, Message Format subsection under Behavior)
- **Read first**: `docs/plans/2026-04-12-03-gemini-formatter.md`
- **Context**: Orchestrator pastes the message format examples from the spec here before launch. Also paste the old service screenshot descriptions:
  - `wx now` with precip: `Td RnShw 58% 57 Tn RnShw 39% 41 Wed ChRn 40% 55 WedN MCldy 18% 43`
  - `wx now` no precip: `Sun 74 TnClr57 Mon 80 Clr59`
  - `wx week`: pack as many day/night periods as fit in 160 chars, truncate from end
- **Owns**: `src/formatter.py`, `tests/test_formatter.py`
- **Must not touch**: `src/email_poller.py`, `src/inreach_parser.py`, `src/nws_client.py`, `src/reply_sender.py`, `src/retry_manager.py`, `src/main.py`, `.env.example`
- **Do not** add `GEMINI_API_KEY` to `.env.example` — just document it in the handoff. Step 4 will consolidate `.env.example`.
- **Important**: Use the `google-generativeai` Python package to call Gemini 3.1 Flash (`gemini-3.1-flash`). The API key comes from `os.environ["GEMINI_API_KEY"]`.
- **Done when**: `format_forecast(command, periods)` calls Gemini, validates ≤160 chars, retries up to 3x. Validation unit tests pass.
- **Handoff**: Write `docs/handoff/step-3-gemini-formatter.md` listing: files created, the `format_forecast` function signature, the Gemini prompt template used, validation rules implemented, and any issues with Gemini output quality.

Stay within your plan's scope. If you see an improvement that belongs to a later step, leave it.

---

**Gate after Steps 1-3**: `python -m pytest tests/ -v` — all parser and formatter validation tests must pass.

**Interface gate after Steps 1-3**: Before launching Step 4, verify:
- [ ] `src/inreach_parser.py` exports a parsed request dataclass with fields: `command`, `latitude`, `longitude`, `message_id`
- [ ] `src/nws_client.py` exports a `fetch_forecast(lat, lon)` function returning forecast period objects
- [ ] `src/formatter.py` exports a `format_forecast(command, periods)` function returning a string

---

### Step 4 — Main Loop + Reply + Retry

**Plan**: `docs/plans/2026-04-12-04-main-loop-reply-retry.md`

**Agent briefing**:
- **Context sources** (orchestrator reads these): `docs/handoff/step-1-email-ingestion.md`, `docs/handoff/step-2-nws-fetch.md`, `docs/handoff/step-3-gemini-formatter.md`, `docs/specs/2026-04-12-01-weather-bot-v1.md` (NWS Retry Flow, Behavior section)
- **Read first**: `docs/plans/2026-04-12-04-main-loop-reply-retry.md`
- **Context**: Orchestrator pastes all three handoff files here, including the interface shapes (dataclass fields, function signatures) from each prior step.
- **Read the source files from Steps 1-3**: `src/email_poller.py`, `src/inreach_parser.py`, `src/nws_client.py`, `src/formatter.py` — understand their actual interfaces before writing integration code.
- **Owns**: `src/reply_sender.py`, `src/retry_manager.py`, `src/main.py`, `tests/test_retry_manager.py`, `.env.example` (add `INREACH_EMAIL` and `GEMINI_API_KEY` to existing file)
- **Must not touch**: `src/email_poller.py`, `src/inreach_parser.py`, `src/nws_client.py`, `src/formatter.py` (except importing from them)
- **Prior step context**: Step 1 created the email poller and parser. Step 2 created the NWS client. Step 3 created the Gemini formatter. Trust the handoff files over this description — read the actual source files to understand the real interfaces.
- **Do not** modify any module from Steps 1-3. If an interface doesn't match what you need, document the mismatch in your handoff and work around it.
- **Done when**: `main.py` runs a poll loop, processes emails, fetches weather, formats via Gemini, sends replies via SMTP, handles NWS retries. Retry manager tests pass. `.env.example` has all 4 vars: `GMAIL_USER`, `GMAIL_APP_PASSWORD`, `GEMINI_API_KEY`, `INREACH_EMAIL`.
- **Handoff**: Write `docs/handoff/step-4-main-loop.md` listing: files created/modified, any integration issues encountered, the complete `.env.example` contents.

Stay within your plan's scope. If you see an improvement that belongs to a later step, leave it.

**Gate**: `python -m pytest tests/ -v` — all tests pass (parser, formatter validation, retry manager).

---

### Step 5 — Deploy to beebaby (HITL)

**Plan**: `docs/plans/2026-04-12-05-deploy-beebaby.md`

> **HITL CHECKPOINT**: Pause here. Confirm with the user:
> 1. All tests pass from Step 4's gate
> 2. `.env` secrets are ready (Gmail App Password, Gemini API key)
> 3. User is ready to deploy

**Agent briefing**:
- **Context sources** (orchestrator reads these): `docs/handoff/step-4-main-loop.md`, `docs/plans/2026-04-12-05-deploy-beebaby.md`
- **Read first**: `docs/plans/2026-04-12-05-deploy-beebaby.md`
- **Context**: Orchestrator pastes Step 4 handoff (especially `.env.example` contents) and the deploy-to-beebaby pattern from `~/dev/hiking-food/deploy/`.
- **Read the pattern exemplar**: `~/dev/hiking-food/deploy/` — skim `deploy.sh`, `setup.sh`, and the `.service` file to match the deployment pattern.
- **Owns**: `deploy/weather-bot.service`, `deploy/deploy.sh`, `deploy/setup.sh`, `.env`, `.env.example`, `.gitignore`, `requirements.txt`
- **Must not touch**: `src/` — all source files are complete.
- **MUST follow the pattern in**: `~/dev/hiking-food/deploy/` — systemd user service, rsync deploy, setup script structure.
- **Key details for the service file**:
  - `ExecStart` should run Python from the venv: `%h/dev/weather-bot/venv/bin/python %h/dev/weather-bot/src/main.py`
  - `EnvironmentFile=%h/dev/weather-bot/.env`
  - `Restart=always`, `RestartSec=5`
  - `Environment=PYTHONUNBUFFERED=1`
- **Key details for deploy.sh**:
  - rsync excludes: `.git`, `__pycache__`, `venv`, `*.pyc`, `.env`
  - After rsync: create/update venv, `pip install -r requirements.txt`
  - Copy service file, daemon-reload, restart
- **Key details for requirements.txt**: `google-generativeai`, `python-dotenv`
- **Done when**: All deploy files exist. Scripts are executable. `.gitignore` covers `.env`, `__pycache__`, `venv`, `*.pyc`.
- **Handoff**: Write `docs/handoff/step-5-deploy.md` listing: files created, the exact bootstrap and deploy commands.

> **HITL CHECKPOINT after Step 5**: User must:
> 1. Fill in `.env` with real credentials
> 2. Run `ssh beebaby 'bash -s' < deploy/setup.sh`
> 3. Run `./deploy/deploy.sh`
> 4. Send a real `wx now` from InReach to verify end-to-end

## HITL checkpoints

- [ ] Before Step 5: confirm tests pass and secrets are ready
- [ ] After Step 5: user deploys and sends a real InReach test message

## Completion criteria

- All plan acceptance criteria met
- `python -m pytest tests/ -v` passes
- Deploy files created and match beebaby pattern
- Real end-to-end test: InReach `wx now` → formatted forecast reply received on device
